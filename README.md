# SUPIT (jQuery)

College project (Internet technology S3): 6 versions of the same web app, built to compare UX and DX. This branch is jQuery + jQuery UI + Bootstrap.

https://supit.k1k1.dev

This branch ships `dist/` only (no `package.json`). Master README has the comparison table, Lighthouse scores, and notes for every version.
